services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=time_capsule_trigger'
      - 'POSTGRES_PASSWORD=1234'
      - 'POSTGRES_USER=postgres'
    ports:
      - '5432'
